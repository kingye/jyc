/target

# Runtime data directory (contains credentials)
.jyc/

# Config files with credentials
config.toml

# Docker env (contains local paths)
docker/.env

# JYC binary and backup (for native s6 supervision)
jyc
jyc.bak
